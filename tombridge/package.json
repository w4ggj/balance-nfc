{
  "name": "tombridge",
  "version": "1.0.0",
  "private": true,
  "description": "Mirror Pokemon TOM HTML reports into Firebase for the Balance Gaming FL live display boards. Read-only against TOM; never writes back.",
  "main": "watcher.js",
  "scripts": {
    "start": "node watcher.js",
    "once": "node watcher.js --once",
    "dry": "node watcher.js --once --dry"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "chokidar": "^3.6.0",
    "firebase-admin": "^12.0.0"
  }
}
